<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktdsuniversity.edu.pms.product.dao.ProductDao">
	
	<select id="getProductAllCount" resultType="_int">
		SELECT COUNT(1)
		  FROM PRODUCT
		 WHERE DEL_YN = 'N'
	</select>
	
	<select id="getAllProduct" resultType="com.ktdsuniversity.edu.pms.product.vo.ProductVO">
		SELECT PRDT_ID
			 , PRDT_NAME
			 , PRDT_CTGR
			 , ONCE_YN
			 , CUR_STR
			 , DEL_YN
		  FROM PRODUCT
		 WHERE DEL_YN = 'N'
	</select>
	
	<insert id="insertNewProduct" parameterType="com.ktdsuniversity.edu.pms.product.vo.ProductVO">
		INSERT INTO PRODUCT
			 (PRDT_ID
			, PRDT_NAME
			, PRDT_CTGR
			, ONCE_YN
			, CUR_STR
			, DEL_YN)
			VALUES
			 ('PRDT_'||TO_CHAR(SYSDATE, 'YYYYMMDD') || '_' || LPAD(SEQ_PRDT_PK.NEXTVAL, 6, '0')
			, #{prdtName}
			, #{prdtCtgr}
			, #{onceYn}
			, #{curStr}
			, 'N')
	</insert>
	
	<select id="selectOneProduct" parameterType="string" resultType="com.ktdsuniversity.edu.pms.product.vo.ProductVO">
		SELECT PRDT_ID
			 , PRDT_NAME
			 , PRDT_CTGR
			 , ONCE_YN
			 , CUR_STR
			 , DEL_YN
		  FROM PRODUCT
		 WHERE PRDT_ID = #{_parameter}
	</select>
</mapper>